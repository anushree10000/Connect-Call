import 'package:flutter/material.dart';

/// Placeholder — next step is wiring this to a Firestore user list stream,
/// search, presence dots, and audio/video call buttons per the plan.
class ContactsScreen extends StatelessWidget {
  const ContactsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Contacts — coming next')),
    );
  }
}
